properties(
    [[$class: 'BuildDiscarderProperty',strategy: 
     [$class: 'LogRotator', numToKeepStr: '10']],
      pipelineTriggers([pollSCM('H/1 * * * *')]),]
)
pipeline {
    agent any

    stages {
        // stage('Thunder_Image_Build') {
        //     steps {
        stage('Checkout') {
            steps {
                checkout([$class: 'GitSCM', branches: [[name: 'master']], doGenerateSubmoduleConfigurations: false,
                        extensions: [[$class: 'CloneOption', timeout: 10, noTags: true, shallow: false]]
                                    +[[$class: 'LocalBranch', localBranch: '']],
                        submoduleCfg: [], userRemoteConfigs: [[refspec: 'alpine_build', url: "${PS_GIT_URI}"]]])
            }
        }
    }
}
