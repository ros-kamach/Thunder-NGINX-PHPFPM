pipeline {
  agent {}
  
  stages {
    stage('Build') {
      steps{
        openshiftBuild(namespace: 'thunder', buildConfig: 'thunder-alpine', showBuildLogs: 'true',  waitTime: '3000000')
      }
    }
  }
}
